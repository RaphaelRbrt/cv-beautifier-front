import { CSSProperties } from 'react'

export const mainContainer: CSSProperties = {
  padding: 16,
  position: 'relative',
}

export const heading: CSSProperties = {
  marginTop: 12,
}

export const stepsContainer: CSSProperties = {
  marginTop: 16,
}

export const loaderContainer: CSSProperties = {
  padding: 16,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  minHeight: '100vh',
}

export const loaderContent: CSSProperties = {
  textAlign: 'center',
}
